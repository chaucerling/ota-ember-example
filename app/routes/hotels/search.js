import Ember from 'ember';

export default Ember.Route.extend({
  // activate: function (argument) {
  //   if ($('#wrapper') != undefined) {
  //     pullScroll = $('#wrapper').pullScroll({}, hotelDao.queryHotels);
  //   };
  // }
});
