import { dateToday } from '../../../helpers/date-today';
import { module, test } from 'qunit';

module('Unit | Helper | date today');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = dateToday(42);
  assert.ok(result);
});
