import { hotelImg } from '../../../helpers/hotel-img';
import { module, test } from 'qunit';

module('Unit | Helper | hotel img');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = hotelImg(42);
  assert.ok(result);
});
